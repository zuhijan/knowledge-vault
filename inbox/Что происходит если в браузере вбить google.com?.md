Сначала нужно рассказывать СКРУПНА:
1. Браузер ищет сервер для обмена данными (DNS)
2. Установка TCP соединения 
3. HTTP запрос за контентом
4. Сервер обрабатывает запрос
5. Ответ браузеру и отрисовка
 
 

- вводим доменное имя  
- нужно понять с чем связано это имя (связано с ДНС адресом ?) 
	-ищется ip-адрес в сети
	- ишется в кэше браузере(?)
	- кэш операционное системы
	- в кэше роутера
	- находится реестр ДНС адресов (где находится?) 
- Знаем где находится сервер
- Устанавливаем Тройное рукопожатие
	- Как работает Тройное рукопожатие (привет/ответ)
	- это часть TCP соединения?
- Приходим на веб-сервер (что это?)
	- обрабатывает маршруты NGINX/APACH
	- Мидлвары и проверка автризации
- Возвращаются данные
	- Пакетами? какие данные в первую очередь?
- HTML сначала запрашивается
- Парсинг HTML
- Ссылки на другие ресурсы - CSS, JS
- Когда данные получили:
	- DOM /CSSCOM
	- - рендер дерево (render tree) - это процесс объединения DOM/CCSOM
	- Строится Лейаут  - расположение элементов на странице
	- Потом идет отрисовка (painting) - накладываются цвета
	- Композитинг слои (что за слои) (z-index)


Вопросы:
- Что если введешь не доменное имя? (не адрес, а запрос)
	- запускается внутреняя функция поиска 



### 1. Браузер ищет сервер для обмена данными (DNS)

### 2. Установка TCP соединени

	- Как можно "увидеть" как устанавливается соединение?

###  3. HTTP запрос за контентом

### 4. Сервер обрабатывает запрос
- чем отличается "веб-сервер" от просто сервера?
-
### 5. Ответ браузеру и отрисовка
	- как можно визуально это воспроизвести?